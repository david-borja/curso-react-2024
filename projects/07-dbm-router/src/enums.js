export const EVENTS = {
  PUSHSTATE: 'pushstate', // este es un evento personalizado, lo podemos llamar como queramos
  POPSTATE: 'popstate' // evento nativo del navegador
}

export const BUTTONS = {
  PRIMARY: 0
}
