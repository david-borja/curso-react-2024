import{jsx as _jsx}from"react/jsx-runtime";import{Children}from"react";import{match}from"path-to-regexp";import{useLocation}from"../hooks/useLocation";export function Router({children,routes=[],defaultComponent:DefaultComponent=()=>_jsx("h1",{children:"404 Not Found"})}){const{currentPath}=useLocation();let routeParams={};const routesFromChildren=Children.map(children,child=>{const{props,type}=child;const{name}=type;const isRoute=name==="Route";return isRoute?props:null});const routesToUse=routes.concat(routesFromChildren).filter(Boolean);const Page=routesToUse.find(({path})=>{if(path===currentPath)return true;const matcherUrl=match(path,{decode:decodeURIComponent});const matched=matcherUrl(currentPath);if(!matched)return false;routeParams=matched.params;return true})?.Component;return Page?_jsx(Page,{routeParams:routeParams}):_jsx(DefaultComponent,{routeParams:routeParams})}